<%- include('../partials/html-head') %>
<link rel="stylesheet" href="/stylesheets/index/choose.css" />
<%- include("../partials/nav") %> 
</head>
<body> 
            <div class="row">
                <div>
                <h1>Profile Information</h1>
                <img src="<%= profile.avatar %> " alt="<%= profile.name %>'s avatar">
                <h2><%= profile.name %></h2>
                <p>Age: <%= profile.age %></p>
                <p>Gender: <%= profile.gender %></p>
                <p>Location: <%= profile.country %>, <%= profile.state %>, <%= profile.city %></p>
                <p>Favorite Bird: <%= profile.favBird %></p>
                <p>Birthday: <%= profile.birthday %></p>
                <p>Biography: <%= profile.bio %></p>
                </div>
            </div>

            <% if (user.profile._id.toString() === profile._id.toString()) { %>
              <a href="/date/<%= profile._id %>/schmedit"><button class="btn btn-lg btn-primary">Edit Profile</button></a>
           
            <form action="https://www.goodnes.com/hot-pockets/products/hickory-ham-cheddar-5-pack/" method="POST">
              <button type="submit" class="btn btn-lg btn-primary">Special Offers</button>
            </form>
<% } %>


<%- include('../partials/footer') %>